# Sminex AutoML v0.25 ULTIMATE - ФИНАЛЬНЫЕ ЗАВИСИМОСТИ
# ✅ ВСЕ ИСПРАВЛЕНИЯ: Совместимые версии без конфликтов, все протестировано

# Core
streamlit==1.28.1
pandas==2.0.3
numpy==1.24.4
scipy==1.11.3
scikit-learn==1.3.1
joblib==1.3.2

# ✅ ДОБАВЛЕНО: Детектирование кодировки (критически важно для разных файлов)
chardet==5.2.0

# Boosting (опционально, но рекомендуется для лучшего качества)
xgboost==1.7.6
lightgbm==4.0.0
catboost==1.2.1

# Visualization
plotly==5.17.0

# Excel support
openpyxl==3.1.0
xlsxwriter==3.1.2

# Optimization & Hyperparameter tuning (опционально для точного режима)
optuna==3.3.0

# Utils
python-dateutil==2.8.2

# Security & Performance
cryptography==41.0.3
pyarrow==13.0.0

# Development (опционально, раскомментируйте если нужно)
# pytest==7.4.2
# black==23.9.1
# flake8==6.1.0
# mypy==1.5.1

# ============================================
# ПРИМЕЧАНИЯ ПО УСТАНОВКЕ:
# ============================================
#
# Минимальная установка (без бустинга и optuna):
#   pip install streamlit pandas numpy scipy scikit-learn joblib chardet plotly openpyxl
#
# Рекомендуемая установка (с бустингом):
#   pip install -r requirements.txt
#
# Для точного режима обязательно нужна optuna:
#   pip install optuna
#
# Если на Windows возникают проблемы с catboost:
#   pip install catboost --no-deps
#
# ============================================
# ВЕРСИИ PYTHON:
# ============================================
# Протестировано на Python 3.9, 3.10, 3.11
# Рекомендуется Python 3.10 для максимальной совместимости
